# ── Streamlit Cloud frontend requirements ─────────────────────────────────────
# Place this file at the ROOT of your repo (same level as frontend/app.py)
# NOT inside agentic_analyst/ — Streamlit Cloud only reads root-level requirements.txt

streamlit>=1.35.0
requests>=2.32.0
plotly>=5.22.0
pandas>=2.2.0
openpyxl>=3.1.0
